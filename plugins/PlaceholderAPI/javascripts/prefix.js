function prefix() {

    if ("%luckperms_prefix%" == "admin") {
        return "%img_admin% "
    }
    if ("%luckperms_prefix%" == "player") {
        return "%img_player% "
    }
    if ("%luckperms_prefix%" == "sglt") {
        return "%img_sglt% "
    }
    if ("%luckperms_prefix%" == "redece") {
        return "%img_redece% "
    }
    if ("%luckperms_prefix%" == "mnn") {
        return "%img_mnn% "
    }
}

prefix()